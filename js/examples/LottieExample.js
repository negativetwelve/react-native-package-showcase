// Libraries
import React from 'react';
import {StyleSheet, View} from 'react-native';

// TODO(mark): Lottie is transpiled which is causing issues with jest.
// import Lottie from 'lottie-react-native';


const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});


// TODO(mark): We need to add a example components from lottie-react-native.
export default () => (
  <View />
);
