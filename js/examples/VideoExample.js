// Libraries
import React from 'react';
import {StyleSheet, View} from 'react-native';
import Video from 'react-native-video';


const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});


// TODO(mark): We need to add a sample video to the repo.
export default () => (
  <View />
);
